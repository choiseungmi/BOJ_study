#include <vector>
#include <iostream>
#include <cmath>
using namespace std;

int main() {
	cin.tie(0);
	cin.sync_with_stdio(false);
	
	int n, m, k;
	cin >> n >> m >> k;
	int ans = 0;
	while (m!=k) {
		m = (m + 1) / 2;
		k = (k + 1) / 2;
		ans++;
	}
	cout << ans;
	return 0;
}
